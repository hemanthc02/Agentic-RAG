# packages/ui-library — design system

Reusable React components (shadcn/ui + Tailwind) implementing the design system
in `documentcreation/ui-ux-specification.md`: tokens (color/space/type), dark
mode, the `ClaimCard`/`SupportednessBadge`/`CitedChunk` verification components,
charts, skeletons, and form primitives. Consumed by `apps/web`. Built in step 13.
