# Multi-Agent RAG — pinned minimums (CPU only, no GPU/CUDA wheels).
# Install into a clean venv:  pip install -r requirements.txt
# Torch must be the CPU build; install it first from the CPU index if needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- Core ML / embeddings / NLI (CPU) ---
torch>=2.2.0
sentence-transformers>=2.7.0
transformers>=4.40.0
faiss-cpu>=1.8.0

# --- Orchestration ---
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.6
langchain-ollama>=0.1.0

# --- LLM backends ---
groq>=0.9.0

# --- PDF parsing ---
PyMuPDF>=1.24.0

# --- Structured data / config ---
pydantic>=2.7.0
python-dotenv>=1.0.0

# --- Evaluation ---
ragas>=0.1.9
datasets>=2.19.0
scipy>=1.11.0            # paired t-test p-values in src/evaluation.py

# --- Auth ---
bcrypt>=4.1.0
PyJWT>=2.8.0

# --- HTTP client (status endpoint pings) ---
httpx>=0.27.0

# --- Gemini, NVIDIA NIM, OpenRouter ---
google-generativeai>=0.7.0
openai>=1.40.0            # NVIDIA NIM + OpenRouter share the OpenAI-compatible SDK

# --- Backend API (FastAPI + React frontend; replaces the Streamlit UI) ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # PDF upload support for /api/ingest
pydantic-settings>=2.3.0

# --- Dev / tooling ---
pytest>=8.2.0
black>=24.4.0
ruff>=0.4.0
