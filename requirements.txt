# Multi-Agent RAG — dependency MINIMUMS (CPU only, no GPU/CUDA wheels).
# Uses ">=" so it installs on any machine with a compatible Python; exact
# pins broke on other computers when a specific version wasn't published for
# their Python. Install into a clean venv:  pip install -r requirements.txt
# Torch CPU build is installed first by START.bat from the CPU wheel index.

# --- Core ML / embeddings / NLI (CPU) ---
torch>=2.2.0
sentence-transformers>=2.7.0
transformers>=4.40.0
faiss-cpu>=1.8.0

# --- Orchestration ---
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0

# --- LLM backends ---
groq>=0.9.0
anthropic>=0.40.0        # Claude cloud backend (online mode)

# --- PDF parsing ---
PyMuPDF>=1.24.0

# --- Structured data / config ---
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# --- Evaluation ---
scipy>=1.11.0            # paired t-test p-values in src/evaluation.py

# --- Auth ---
bcrypt>=4.1.0
PyJWT>=2.8.0
email-validator>=2.1.0   # required by pydantic EmailStr in auth_router

# --- HTTP client (status endpoint pings, paper download) ---
httpx>=0.27.0

# --- Backend API (FastAPI + React frontend) ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9  # PDF upload support

# --- Azure managed services (only needed when the matching *_BACKEND is set to
#     azure; safe to install everywhere — imports are lazy) ---
azure-storage-blob>=12.19.0   # STORAGE_BACKEND=azure_blob
azure-search-documents>=11.4.0 # SEARCH_BACKEND=azure_search (vector search)
pyodbc>=5.1.0                  # DB_BACKEND=azure_sql (needs an ODBC SQL driver)

# --- Dev / tooling ---
pytest>=8.0.0
